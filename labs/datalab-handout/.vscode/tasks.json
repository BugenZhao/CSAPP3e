{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "btest",
            "type": "shell",
            "command": "make clean && make btest && ./btest"
        },
        {
            "label": "dlc",
            "type": "shell",
            "command": "./dlc -e ./bits.c"
        }
    ]
}
